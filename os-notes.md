# 操作系统原理 — 结构化知识笔记

> **课程**：Operating Systems Principles  
> **整理日期**：2026年6月3日  
> **说明**：基于中山大学张青老师课件整理，去除水分内容，保留核心知识点

---

## 目录

1. [第一章 计算机系统概述](#第一章-计算机系统概述)
2. [第二章 操作系统结构](#第二章-操作系统结构)
3. [第三章 中断和异常](#第三章-中断和异常)
4. [第四章 进程描述和控制](#第四章-进程描述和控制)
5. [第五章 线程](#第五章-线程)
6. [第六章 进程调度](#第六章-进程调度)
7. [第七章 同步](#第七章-同步)
8. [第八章 死锁](#第八章-死锁)
9. [第九章 物理内存管理](#第九章-物理内存管理)
10. [第十章 虚拟内存管理](#第十章-虚拟内存管理)
11. [第十一章 大容量存储](#第十一章-大容量存储)
12. [第十二章 I/O系统](#第十二章-io系统)
13. [第十三章 文件系统](#第十三章-文件系统)
14. [第十四章 文件系统实现](#第十四章-文件系统实现)

---

# 第一章 计算机系统概述

## 1. 概念引入

**操作系统（Operating System, OS）** 是管理计算机硬件与软件资源的程序，是计算机系统中最基本的系统软件。它为应用程序提供方便、安全、一致的接口，可想象为资源的统一抽象表示。

**冯·诺依曼体系结构（Von Neumann Architecture）**：程序存储在内存中，CPU 按"取指-译码-执行"循环工作。核心组件包括 CPU（运算器+控制器）、内存（主存储器）、输入/输出设备。

**存储层次（Memory Hierarchy）**：从快到慢、从小到大依次为：
- 寄存器（Register）→ 高速缓存（L1/L2/L3 Cache）→ 主存（RAM/DRAM）→ 磁盘（Disk/HDD）→ 固态硬盘（SSD）
- 越靠近 CPU 的存储越快但越贵、容量越小

## 2. 核心原理

**CPU 执行周期**：取指（Fetch）→ 译码（Decode）→ 执行（Execute）→ 写回（Write Back），不断重复。

**指令周期时间**：一条指令的执行时间 = 取指时间 + 译码时间 + 执行时间 + 访存时间（如有）。

**I/O 操作**：
- CPU 与 I/O 设备速度差距巨大
- I/O 方式：程序控制 I/O（轮询）→ 中断驱动 I/O → DMA（直接内存访问）
- DMA 允许设备直接与内存交换数据，不需 CPU 逐字节参与

**总线（Bus）**：连接 CPU、内存、I/O 设备的通信通道，分为数据总线、地址总线、控制总线。

## 3. 方法速通

- **理解存储层次的关键**：局部性原理（时间局部性 + 空间局部性）是 Cache 有效工作的基础
- **计算内存访问时间**：命中率 H、Cache 访问时间 Tc、主存访问时间 Tm → 平均访问时间 = H×Tc + (1-H)×Tm
- **I/O 方式对比**：轮询（CPU 忙等）→ 中断（CPU 可做其他事）→ DMA（CPU 几乎不参与数据传输）

## 4. 典型示例

**存储层次计算**：设 Cache 命中率 95%，Cache 访问 10ns，主存访问 200ns：
- 平均访问时间 = 0.95 × 10 + 0.05 × 200 = 9.5 + 10 = 19.5ns

**DMA 传输过程**：
1. CPU 向 DMA 控制器设置传输参数（源地址、目标地址、字节数）
2. DMA 控制器接管总线，直接在设备和内存间传输数据
3. 传输完成后，DMA 向 CPU 发中断通知

## 5. 真题/实战

- Linux 系统中使用 `/proc/cpuinfo` 查看 CPU 信息，`free` 查看内存使用
- 现代处理器通常采用多级 Cache（L1 通常 32-64KB，L2 256KB-1MB，L3 数 MB）
- SSD vs HDD：SSD 无机械部件，随机读写速度快 100 倍以上，但写入次数有限

## 6. 巩固练习

**Q1**：为什么 CPU 不直接访问磁盘？  
**A1**：速度差距太大（CPU 纳秒级 vs 磁盘毫秒级），直接访问会导致 CPU 大量空等。需要通过存储层次和 DMA 等机制来弥合速度差距。

**Q2**：解释程序从源代码到运行的完整流程。  
**A2**：预处理（展开宏/include）→ 编译（生成汇编）→ 汇编（生成目标文件 .o）→ 链接（合并目标文件和库，生成可执行文件）→ 加载（操作系统将可执行文件载入内存）→ 执行。

## 7. 总结复盘

- 操作系统的核心角色：资源管理者 + 用户接口
- 存储层次利用局部性原理弥合 CPU 与存储的速度差距
- I/O 方式的演进体现了"让 CPU 从繁琐工作中解放"的趋势
- **易错点**：混淆逻辑地址与物理地址（后续章节详述）；忽略 DMA 中 CPU 仍需设置参数的阶段

---

# 第二章 操作系统结构

## 1. 概念引入

**内核态（Kernel Mode / System Mode）** 与 **用户态（User Mode）**：
- 用户态：应用程序运行的受限模式，不能直接访问硬件和内核数据
- 内核态：操作系统内核运行的特权模式，可执行任何指令、访问任何内存
- CPU 通过 **模式位（Mode Bit）** 区分：0 = 内核态，1 = 用户态

**系统调用（System Call）**：用户程序请求操作系统服务的接口。执行系统调用时，CPU 从用户态切换到内核态，处理完成后返回用户态。

## 2. 核心原理

**系统调用的执行流程**：
1. 用户程序调用库函数（如 `printf`）
2. 库函数将系统调用号放入寄存器，执行 `trap` 指令（或 `int 0x80` / `syscall`）
3. CPU 切换到内核态，跳转到中断向量表中对应的系统调用处理程序
4. 内核执行服务（如写入文件）
5. 返回用户态，继续执行

**操作系统结构分类**：

| 结构 | 特点 | 优点 | 缺点 |
|------|------|------|------|
| **宏内核（Monolithic）** | 所有 OS 功能在内核空间运行 | 性能高，模块间调用直接 | 一个模块崩溃可能导致整个内核崩溃 |
| **微内核（Microkernel）** | 只有最基本功能在内核，其余在用户空间 | 可靠性高，模块化好 | 消息传递开销大，性能较低 |
| **混合内核（Hybrid）** | 结合两者，部分服务放内核 | 兼顾性能和可靠性 | 设计复杂 |
| **外核（Exokernel）** | 内核只做资源隔离，策略由应用决定 | 灵活性极高 | 实现复杂 |

**系统调用分类**：
- 进程管理：`fork()`, `exec()`, `exit()`, `wait()`
- 文件管理：`open()`, `read()`, `write()`, `close()`
- 设备管理：`ioctl()`, `read()`, `write()`
- 信息维护：`getpid()`, `alarm()`, `sleep()`
- 通信：`pipe()`, `shmget()`, `mmap()`

## 3. 方法速通

- 区分宏内核和微内核的关键：看系统调用是"直接函数调用"还是"消息传递"
- Linux 是宏内核（但通过可加载内核模块 LKM 提供一定灵活性）
- Windows 是混合内核，macOS/XNU 也是混合内核（Mach 微内核 + BSD 宏内核）
- Minix、QNX 是典型微内核

## 4. 典型示例

**Shell 执行 `ls` 命令的过程**：
1. Shell 调用 `fork()` 创建子进程
2. 子进程调用 `exec("ls")` 加载 ls 程序
3. ls 程序通过 `write()` 系统调用输出结果到终端
4. 父进程（Shell）调用 `wait()` 等待子进程结束

## 5. 真题/实战

- Android 使用 Linux 宏内核
- iOS/macOS 使用 XNU 混合内核
- L4 微内核家族在嵌入式系统广泛使用
- 容器技术（Docker）依赖内核的 namespace 和 cgroup 机制

## 6. 巩固练习

**Q1**：为什么系统调用比普通函数调用开销大？  
**A1**：系统调用需要：①模式切换（用户态→内核态→用户态）；②保存/恢复寄存器上下文；③可能涉及内核栈切换。而普通函数调用只需压栈跳转。

**Q2**：微内核的"消息传递"指什么？为什么性能较低？  
**A2**：微内核中，用户空间的服务（如文件系统、驱动）之间或与内核之间通过消息（message）通信，需要：序列化→发送→内核转发→接收→反序列化。每次通信都涉及上下文切换和数据拷贝，开销大于宏内核中的直接函数调用。

## 7. 总结复盘

- 用户态/内核态切换是理解 OS 的基础
- 系统调用是用户与内核交互的唯一合法途径
- 宏内核 vs 微内核是架构设计的根本权衡：性能 vs 可靠性
- **易错点**：误以为库函数 = 系统调用（库函数可能在用户态完成，不触发系统调用）；混淆 trap/中断/异常的概念（下章详述）

---

# 第三章 中断和异常

## 1. 概念引入

**中断（Interrupt）**：来自 CPU 外部硬件的信号，与当前执行的指令无关（异步）。如键盘输入、网络数据到达、定时器到期。

**异常（Exception）**：由 CPU 执行指令过程中内部产生，与当前指令相关（同步）。如除零错误、缺页异常、系统调用。

**关键区别**：

| 特征 | 中断 | 异常 |
|------|------|------|
| 来源 | 外部硬件 | CPU 内部（当前指令） |
| 同步性 | 异步 | 同步 |
| 是否可屏蔽 | 可屏蔽/不可屏蔽 | 不可屏蔽 |
| 返回行为 | 通常返回下一条指令 | 可能返回/终止 |

## 2. 核心原理

**异常的分类（Linux/x86）**：
- **Fault（故障）**：可恢复，重新执行引发异常的指令。典型：缺页异常（Page Fault）
- **Trap（陷阱）**：无需恢复，执行下一条指令。典型：断点（int 3）、系统调用（int 0x80/syscall）
- **Abort（终止）**：严重错误，不可恢复。典型：机器检查异常

**中断处理流程**：
1. 硬件：CPU 完成当前指令 → 检查中断信号 → 关中断
2. 保存现场：将 PC、PSW、寄存器压入内核栈
3. 根据中断向量号查 **中断向量表（IVT / IDT）**，跳转到对应处理程序
4. 执行中断服务程序（ISR）
5. 恢复现场：从内核栈弹出寄存器
6. 开中断，返回被中断的指令继续执行

**中断向量表（Interrupt Descriptor Table, IDT）**：
- 每个表项包含中断处理程序的入口地址和特权级信息
- 位于内核内存中，由操作系统初始化
- x86-64 中 IDT 最多 256 个表项

## 3. 方法速通

**Top Half / Bottom Half 机制**（Linux）：
- **Top Half**：中断处理的上半部，做最少的工作（保存寄存器、屏蔽中断、读取硬件状态、设置标志位），尽快返回
- **Bottom Half**：中断处理的下半部，推迟执行非关键工作，可被新中断打断
- Bottom Half 实现方式：softirq（软中断）、tasklet（基于 softirq）、工作队列（work queue）、内核线程

**中断处理的约束**：
- 不能睡眠或调用可能睡眠的函数
- 不能调用 `schedule()` 进行进程调度
- 不能与用户地址空间交换数据
- 原因：中断处理没有进程上下文

## 4. 典型示例

**网络数据包到达的中断处理**：
1. 网卡收到数据包 → 向 CPU 发中断
2. Top Half：读取网卡状态，将数据包拷贝到内核缓冲区，标记 softirq
3. Bottom Half（softirq）：处理协议栈、将数据包递交给上层应用

**系统调用的中断机制**：
- 用户程序执行 `int 0x80`（x86）或 `syscall`（x86-64）
- CPU 切换到内核态，查 IDT 跳转到系统调用入口
- 从寄存器读取系统调用号和参数，执行对应服务

## 5. 真题/实战

- Linux 查看中断信息：`cat /proc/interrupts`
- 中断亲和性（IRQ affinity）：可设置特定 CPU 处理特定中断，优化多核性能
- 嵌入式系统中实时中断响应时间是关键指标

## 6. 巩固练习

**Q1**：缺页异常属于 Fault、Trap 还是 Abort？  
**A1**：属于 Fault。缺页异常是可恢复的——操作系统将所需页面调入内存后，重新执行引发异常的指令。

**Q2**：为什么中断处理程序中不能睡眠？  
**A2**：因为中断处理在中断上下文中执行，没有关联的进程上下文。睡眠意味着需要调度其他进程，但此时没有"当前进程"可供调度回来。

## 7. 总结复盘

- 中断 vs 异常：来源不同（外部 vs 内部）、同步性不同
- Fault/Trap/Abort：可恢复/无需恢复/不可恢复
- Top Half 做最少的事快速返回，Bottom Half 推迟处理
- **易错点**：混淆中断和异常的同步性；误以为系统调用是函数调用（实际通过中断/陷阱指令实现）

---

# 第四章 进程描述和控制

## 1. 概念引入

**进程（Process）**：程序的一次执行实例。是操作系统进行资源分配和调度的基本单位。

**进程 vs 程序**：
- 程序（Program）：静态的代码文件（如 hello.c 编译后的可执行文件）
- 进程（Process）：动态的执行过程，包含程序代码 + 数据 + 堆栈 + 系统资源

**进程控制块（Process Control Block, PCB）**：操作系统为每个进程维护的数据结构，包含：
- 进程标识符（PID）
- 进程状态（State）
- 程序计数器（PC）
- CPU 寄存器值
- 内存管理信息（页表基址等）
- I/O 状态信息（打开文件表）
- 调度信息（优先级、时间片）

## 2. 核心原理

**进程状态模型**：

**五状态模型**：
```
新建(New) → 就绪(Ready) ⇄ 运行(Running) → 终止(Terminated)
                ↑              ↓
                └── 等待/阻塞(Waiting/Blocked) ←─┘
```
- **New**：进程正在被创建（分配 PCB、分配内存）
- **Ready**：等待 CPU 调度
- **Running**：正在 CPU 上执行
- **Waiting/Blocked**：等待某事件（如 I/O 完成、信号量）
- **Terminated**：执行完毕，等待回收资源

**七状态模型**（增加挂起）：
- 在五状态基础上增加 **就绪挂起（Ready/Suspend）** 和 **等待挂起（Blocked/Suspend）**
- 挂起的进程被换出到磁盘（swap），释放物理内存

## 3. 方法速通

**关键系统调用**：

| 系统调用 | 功能 | 返回值 |
|----------|------|--------|
| `fork()` | 创建子进程 | 父进程返回子进程 PID，子进程返回 0 |
| `exec*()` | 替换进程映像 | 成功不返回，失败返回 -1 |
| `exit()` | 终止当前进程 | 无返回 |
| `wait()` | 等待子进程终止 | 返回终止子进程的 PID |

**fork() 执行流程（内核层面）**：
1. 在进程表中为子进程分配新条目
2. 赋予子进程唯一 PID
3. 复制父进程的用户空间内存（代码段、数据段、堆、栈）
4. 复制父进程的打开文件表
5. 父进程返回子进程 PID，子进程返回 0

**写时拷贝（Copy-on-Write, COW）**：
- fork() 后父子进程共享物理页面，标记为只读
- 任一方尝试写入时，触发缺页异常，内核才真正复制该页面
- 避免了 fork() 后立即 exec() 时的无用拷贝

**exec*() 系列**：
- `execl`, `execlp`, `execle`, `execv`, `execvp`, `execve`
- 只有 `execve` 是真正的系统调用，其余是库函数封装
- 用新程序的代码段、数据段替换当前进程的用户空间，但 PID 不变

## 4. 典型示例

**fork() + exec() 的经典模式**（Shell 执行命令）：
```c
pid_t pid = fork();
if (pid == 0) {
    // 子进程
    execlp("ls", "ls", "-l", NULL);
    perror("exec failed");
    exit(1);
} else if (pid > 0) {
    // 父进程
    wait(NULL);  // 等待子进程结束
    printf("Child done\n");
}
```

**僵尸进程与孤儿进程**：
- **僵尸进程（Zombie）**：子进程已 exit()，但父进程未调用 wait() 回收。子进程的 PCB 仍占进程表条目。
- **孤儿进程（Orphan）**：父进程先于子进程终止，子进程被 init（PID 1）收养。
- 大量僵尸进程会耗尽进程表资源。

## 5. 真题/实战

- Linux 查看进程：`ps aux`、`top`
- 进程信息：`/proc/[PID]/status`、`/proc/[PID]/maps`
- 进程间关系：`pstree` 可视化进程树

## 6. 巩固练习

**Q1**：fork() 一次调用，为什么返回两次？  
**A1**：fork() 在内核中创建子进程后，父子进程各自从 fork() 返回。父进程得到子进程的 PID（正数），子进程得到 0。这是通过内核在两个进程的寄存器中设置不同的返回值实现的。

**Q2**：进程和线程的主要区别是什么？  
**A2**：进程是资源分配的基本单位（拥有独立的地址空间、文件描述符等），线程是 CPU 调度的基本单位（共享进程资源，拥有独立的栈和寄存器状态）。同一进程的多个线程共享代码段、数据段、堆。

## 7. 总结复盘

- PCB 是操作系统管理进程的核心数据结构
- fork() + exec() 是 UNIX 创建新进程的标准模式
- 写时拷贝优化了 fork() 的性能
- **易错点**：fork() 后父子进程执行顺序不确定；忘记处理 fork() 返回值 -1（失败）；exec() 成功后不会返回

---

# 第五章 线程

## 1. 概念引入

**线程（Thread）**：CPU 调度的基本单位，是进程内的一个执行路径。一个进程可包含多个线程，它们共享进程的代码段、数据段、堆和系统资源，但各自拥有独立的栈、程序计数器和寄存器。

**线程控制块（Thread Control Block, TCB）**：与 PCB 类似，包含线程 ID、寄存器状态、栈指针、状态等。

**并发 vs 并行**：
- **并发（Concurrency）**：多个任务在时间上交替执行（单核即可实现）
- **并行（Parallelism）**：多个任务在同一时刻真正同时执行（需要多核）

## 2. 核心原理

**Amdahl 定律**：评估增加处理器核心带来的加速比。
```
加速比 S = 1 / (f + (1-f)/N)
```
其中 f 是串行部分比例，N 是处理器核心数。即使 N→∞，加速比上限为 1/f。

**多线程的优点**：
- **响应性**：部分线程阻塞时其他线程可继续执行
- **资源共享**：线程共享进程资源，比进程间通信更高效
- **经济性**：创建/切换线程比进程开销小
- **可伸缩性**：多线程可利用多核并行执行

## 3. 方法速通

**用户级线程（User-Level Thread, ULT）vs 内核级线程（Kernel-Level Thread, KLT）**：

| 特征 | ULT | KLT |
|------|-----|-----|
| 管理者 | 用户空间线程库 | 操作系统内核 |
| 创建/切换速度 | 快（无需模式切换） | 较慢（需内核介入） |
| 内核感知 | 不感知 | 感知 |
| 一个线程阻塞 | 整个进程阻塞 | 只阻塞该线程 |
| 典型实现 | POSIX Pthreads（用户态） | Linux pthreads（NPTL） |

**多线程模型**：

| 模型 | 描述 | 优点 | 缺点 |
|------|------|------|------|
| 多对一（Many-to-One） | 多个 ULT 映射到一个 KLT | 便于移植 | 一个线程阻塞→全部阻塞 |
| 一对一（One-to-One） | 每个 ULT 映射到一个 KLT | 真正并发 | 创建线程数受限 |
| 多对多（Many-to-Many） | M 个 ULT 映射到 N 个 KLT | 灵活 | 实现复杂 |

- Linux 和 Windows 采用一对一模型
- Solaris 早期采用多对多模型

## 4. 典型示例

**Pthreads 创建线程**：
```c
#include <pthread.h>

void* thread_func(void* arg) {
    printf("Thread %d running\n", *(int*)arg);
    return NULL;
}

int main() {
    pthread_t tid;
    int id = 1;
    pthread_create(&tid, NULL, thread_func, &id);
    pthread_join(tid, NULL);  // 等待线程结束
    return 0;
}
```

**线程池（Thread Pool）**：
- 预先创建一组线程，等待任务分配
- 避免频繁创建/销毁线程的开销
- 任务到来时从池中取空闲线程执行，执行完归还

## 5. 真题/实战

- Linux 线程实现：NPTL（Native POSIX Thread Library），一对一模型
- `top -H` 查看线程级 CPU 使用
- `/proc/[PID]/task/` 目录列出进程的所有线程
- Java 中 `Thread` 类底层调用 pthreads

## 6. 巩固练习

**Q1**：为什么用户级线程中一个线程阻塞会导致整个进程阻塞？  
**A1**：因为内核只看到一个进程/一个内核级线程。当该内核级线程因 I/O 阻塞时，内核不知道还有其他用户级线程可以运行。

**Q2**：Amdahl 定律中，如果程序 25% 是串行的，使用 4 个核心的最大加速比是多少？  
**A2**：S = 1 / (0.25 + 0.75/4) = 1 / (0.25 + 0.1875) = 1/0.4375 ≈ 2.29

## 7. 总结复盘

- 线程是轻量级的执行单元，共享进程资源
- ULT vs KLT 的核心区别在于内核是否感知
- Amdahl 定律揭示了并行加速的上限
- **易错点**：混淆线程和进程的资源共享范围；误以为多线程一定更快（受 Amdahl 定律限制）

---

# 第六章 进程调度

## 1. 概念引入

**CPU 调度（CPU Scheduling）**：从就绪队列中选择一个进程/线程，将 CPU 分配给它。是多道程序操作系统的核心机制。

**调度层次**：
- **长程调度（Long-term）**：决定哪些进程从外存调入内存（作业调度）
- **中程调度（Medium-term）**：决定哪些进程被挂起/换出（交换调度）
- **短程调度（Short-term）**：决定就绪队列中哪个进程获得 CPU（CPU 调度）

**抢占式 vs 非抢占式**：
- **非抢占式（Non-preemptive）**：进程运行直到主动释放 CPU（阻塞或终止）
- **抢占式（Preemptive）**：OS 可以强制剥夺 CPU（时间片用完或更高优先级进程就绪）

## 2. 核心原理

**调度准则**：
- **CPU 利用率**：CPU 忙碌时间比例
- **吞吐量（Throughput）**：单位时间完成的进程数
- **周转时间（Turnaround Time）**：进程从提交到完成的总时间 = 等待时间 + 执行时间 + I/O 时间
- **等待时间（Waiting Time）**：进程在就绪队列中等待的总时间
- **响应时间（Response Time）**：从提交请求到第一次响应的时间

**调度算法对比**：

| 算法 | 类型 | 特点 | 可能导致饥饿 |
|------|------|------|-------------|
| FCFS（先来先服务） | 非抢占 | 简单，但可能产生"护航效应" | 否 |
| SJF（最短作业优先） | 非抢占 | 理论最优平均等待时间 | 是（长作业） |
| SRTF（最短剩余时间） | 抢占 SJF | SJF 的抢占版本 | 是 |
| RR（轮转） | 抢占 | 公平，时间片影响性能 | 否 |
| 优先级调度 | 都可 | 按优先级选择 | 是（低优先级） |
| 多级队列 | 抢占 | 多个独立队列，不同策略 | 视配置 |
| 多级反馈队列 | 抢占 | 最灵活，进程可在队列间移动 | 可能 |

## 3. 方法速通

**SJF 最优性**：SJF 对于给定任务集，平均等待时间最小（可数学证明）。

**指数平均法（预测下一个 CPU 突发时间）**：
```
τ_{n+1} = α × t_n + (1-α) × τ_n
```
- t_n：第 n 次实际 CPU 突发时间
- τ_n：第 n 次预测值
- α：权重（0 < α < 1），α 越大越重视最近的实际值

**轮转调度（RR）时间片选择**：
- 时间片太大 → 退化为 FCFS
- 时间片太小 → 上下文切换开销过大
- 经验值：10-100ms，切换开销应 < 时间片的 1%

**多级反馈队列（MFQ）规则**：
1. 新进程进入最高优先级队列
2. 时间片用完未结束，降到下一级队列
3. 高优先级队列优先调度
4. 低优先级队列时间片更长
5. 可设置"老化（Aging）"机制：等待过久的进程提升优先级，防止饥饿

## 4. 典型示例

**FCFS vs SJF 对比**（进程到达时间均为 0，执行时间 P1=24, P2=3, P3=3）：

FCFS 顺序 P1→P2→P3：
- P1 等待 0，P2 等待 24，P3 等待 27
- 平均等待时间 = (0+24+27)/3 = 17

SJF 顺序 P2→P3→P1：
- P2 等待 0，P3 等待 3，P1 等待 6
- 平均等待时间 = (0+3+6)/3 = 3

**RR 调度**（时间片=4，P1=24, P2=3, P3=3）：
- 0-4: P1 执行 | 4-7: P2 执行(完成) | 7-10: P3 执行(完成) | 10-14: P1 执行...
- P2 和 P3 在第一轮就完成，P1 逐步执行

## 5. 真题/实战

- Linux 调度器：CFS（Completely Fair Scheduler），基于虚拟运行时间（vruntime），使用红黑树
- Windows 调度器：多级反馈队列，32 个优先级级别
- 实时系统调度：RM（Rate Monotonic，静态优先级）和 EDF（Earliest Deadline First，动态优先级）

## 6. 巩固练习

**Q1**：为什么 SJF 在实际系统中难以实现？  
**A1**：SJF 需要知道下一个 CPU 突发的长度，但这无法精确预知。实际系统使用指数平均法近似预测。

**Q2**：多级反馈队列如何防止低优先级进程饥饿？  
**A2**：通过老化（Aging）机制：进程在低优先级队列中等待一定时间后，自动提升其优先级。

## 7. 总结复盘

- 没有"最好"的调度算法，选择取决于系统目标
- SJF 平均等待时间最优但饥饿问题严重
- RR 公平但时间片大小影响性能
- MFQ 最灵活，是实际操作系统最常用的调度策略
- **易错点**：计算 RR 时忘记考虑新到达的进程；混淆抢占式和非抢占式的 SJF

---

# 第七章 同步

## 1. 概念引入

**竞态条件（Race Condition）**：多个进程/线程并发访问共享数据，最终结果取决于执行的精确时序。经典例子：两个进程同时执行 `count++`，可能丢失一次增量。

**临界区问题（Critical Section Problem）**：确保当一个进程在临界区（访问共享资源的代码段）执行时，其他进程不能进入其临界区。

**三个必须满足的条件**：
1. **互斥（Mutual Exclusion）**：同一时刻只有一个进程在临界区
2. **前进（Progress）**：如果没有进程在临界区且有进程想进入，必须允许进入
3. **有限等待（Bounded Waiting）**：等待进入临界区的进程不会无限等待

## 2. 核心原理

**Peterson 算法**（两个进程的软件解决方案）：
```c
// 共享变量
int turn;
bool flag[2] = {false, false};

// 进程 i 的代码（i = 0 或 1，j = 1-i）
flag[i] = true;
turn = j;           // 让对方先
while (flag[j] && turn == j)
    ;               // 忙等
// === 临界区 ===
flag[i] = false;
```
满足互斥、前进、有限等待三个条件，但忙等浪费 CPU。

**硬件同步**：
- **test_and_set()**：原子操作，读取旧值并设置为 true
```c
bool test_and_set(bool *target) {
    bool old = *target;
    *target = true;
    return old;
}
```
- **compare_and_swap() (CAS)**：原子操作
```c
int compare_and_swap(int *value, int expected, int new_value) {
    int temp = *value;
    if (*value == expected) *value = new_value;
    return temp;
}
```

**信号量（Semaphore）**：
- 整型变量 S，只能通过两个原子操作访问：
  - **wait/P 操作**：`S--; if (S < 0) block;`
  - **signal/V 操作**：`S++; if (S <= 0) wakeup;`
- **二值信号量**（互斥锁）：S 初始为 1，用于互斥
- **计数信号量**：S 初始为 N，用于资源计数

**管程（Monitor）**：
- 高级同步构造，封装共享数据和操作
- 同一时刻只有一个进程可以在管程内执行
- 使用条件变量（condition）实现等待/唤醒
- Java 的 `synchronized` 关键字就是管程的一种实现

## 3. 方法速通

**经典同步问题**：

**生产者-消费者问题**：
```c
semaphore mutex = 1;    // 互斥访问缓冲区
semaphore empty = N;    // 空槽位数
semaphore full = 0;     // 满槽位数

// 生产者                    // 消费者
wait(empty);                wait(full);
wait(mutex);                wait(mutex);
// 放入缓冲区               // 从缓冲区取出
signal(mutex);              signal(mutex);
signal(full);               signal(empty);
```

**读者-写者问题**：
- 多个读者可同时读，写者独占
- 第一个读者要等写者完成，后续读者可直接加入
- 使用 `readcount` 变量 + 互斥锁 + 读写锁

**哲学家就餐问题**：
- 5 个哲学家围坐，交替思考和进餐
- 每人需要左右两只筷子才能吃饭
- 解决方案：限制同时进餐人数 / 奇数先拿左偶数先拿右 / 使用管程

## 4. 典型示例

**用信号量实现互斥**：
```c
semaphore mutex = 1;

// 进程 P1
wait(mutex);
// 临界区
signal(mutex);

// 进程 P2
wait(mutex);
// 临界区
signal(mutex);
```

**忙等 vs 阻塞**：
- 忙等（自旋锁）：`while (!available);` 浪费 CPU，但适合短等待
- 阻塞（信号量）：进程被挂起，让出 CPU，适合长等待
- 多核系统中自旋锁可能优于信号量（避免上下文切换开销）

## 5. 真题/实战

- Linux 内核同步原语：自旋锁（spinlock）、互斥锁（mutex）、信号量（semaphore）、RCU
- Java 并发：`synchronized`（管程）、`ReentrantLock`、`Semaphore`、`CountDownLatch`
- 死锁是同步机制的"副作用"——互斥 + 持有并等待可能造成死锁（下章详述）

## 6. 巩固练习

**Q1**：Peterson 算法中 `turn` 变量的作用是什么？  
**A1**：解决两个进程同时设置 `flag` 为 true 的竞争。`turn` 最后被设置的一方让步，确保至少一个进程能进入临界区。

**Q2**：为什么信号量的 wait 和 signal 操作必须是原子的？  
**A2**：如果 wait 不是原子的，两个进程可能同时读到 S=1，都将其减为 0 并进入临界区，破坏互斥。

## 7. 总结复盘

- 同步的核心是解决并发访问共享资源的正确性
- 从软件（Peterson）到硬件（CAS）到高级抽象（信号量、管程），抽象层次递增
- 经典同步问题（生产者-消费者、读者-写者、哲学家就餐）是理解同步机制的标准案例
- **易错点**：信号量的 wait/signal 操作顺序错误导致死锁；生产者-消费者中 mutex 的 wait 必须在 empty/full 的 wait 之后

---

# 第八章 死锁

## 1. 概念引入

**死锁（Deadlock）**：一组进程中的每个进程都在等待该组中其他进程释放资源，导致所有进程都无法继续执行。

**死锁的四个必要条件**（Coffman 条件）：
1. **互斥（Mutual Exclusion）**：资源一次只能被一个进程使用
2. **持有并等待（Hold and Wait）**：进程持有资源的同时等待其他资源
3. **不可抢占（No Preemption）**：已分配的资源不能被强制剥夺
4. **循环等待（Circular Wait）**：存在进程的循环等待链

## 2. 核心原理

**资源分配图（Resource Allocation Graph）**：
- 节点：进程（圆圈）和资源（方块，内含小圆表示实例数）
- 请求边：进程 → 资源（请求分配）
- 分配边：资源 → 进程（已分配）
- **有环不一定死锁**（单实例资源有环 = 死锁，多实例有环不一定）

**死锁处理策略**：

| 策略 | 时机 | 方法 |
|------|------|------|
| 死锁预防 | 事前 | 破坏四个必要条件之一 |
| 死锁避免 | 事中 | 银行家算法 |
| 死锁检测+恢复 | 事后 | 检测死锁后采取措施 |

## 3. 方法速通

**死锁预防（破坏条件）**：
- 破坏互斥：不可行（资源本质互斥）
- 破坏持有并等待：进程一次申请所有资源（浪费、低效）
- 破坏不可抢占：允许抢占资源（可能导致回滚）
- 破坏循环等待：对资源编号，按序申请

**银行家算法（Banker's Algorithm）**：

核心数据结构：
- **Available[m]**：每种资源的可用数量
- **Max[n][m]**：每个进程的最大需求
- **Allocation[n][m]**：每个进程已分配的资源
- **Need[n][m]**：每个进程还需要的资源（= Max - Allocation）

安全算法步骤：
1. 设 Work = Available, Finish[i] = false
2. 找一个 i：Finish[i]==false 且 Need[i] ≤ Work
3. 若找到：Work += Allocation[i], Finish[i] = true, 回到步骤 2
4. 若所有 Finish[i] 都为 true → 系统安全，存在安全序列
5. 若找不到这样的 i → 系统不安全

## 4. 典型示例

**银行家算法实例**：
- 3 类资源 A(10), B(5), C(7)
- 当前 Available = (3, 3, 2)
- 进程 P0~P4 的 Allocation 和 Need 如下：

| 进程 | Allocation | Need | Max |
|------|-----------|------|-----|
| P0 | 0,1,0 | 7,4,3 | 7,5,3 |
| P1 | 2,0,0 | 1,2,2 | 3,2,2 |
| P2 | 3,0,2 | 6,0,0 | 9,0,2 |
| P3 | 2,1,1 | 0,1,1 | 2,2,2 |
| P4 | 0,0,2 | 4,3,1 | 4,3,3 |

安全序列检查：Work=(3,3,2)
1. P1: Need(1,2,2) ≤ Work(3,3,2) ✓ → Work=(5,3,2), Finish[1]=true
2. P3: Need(0,1,1) ≤ Work(5,3,2) ✓ → Work=(7,4,3), Finish[3]=true
3. P4: Need(4,3,1) ≤ Work(7,4,3) ✓ → Work=(7,4,5), Finish[4]=true
4. P2: Need(6,0,0) ≤ Work(7,4,5) ✓ → Work=(10,4,7), Finish[2]=true
5. P0: Need(7,4,3) ≤ Work(10,4,7) ✓ → Work=(10,5,7), Finish[0]=true

安全序列：<P1, P3, P4, P2, P0> ✓

## 5. 真题/实战

- 数据库系统中广泛使用死锁检测：等待图（Wait-for Graph）有环则死锁
- Linux 中可设置 `/proc/sys/kernel/hung_task_timeout_secs` 检测进程长时间阻塞
- 实时系统中优先使用死锁预防（可预测性更重要）

## 6. 巩固练习

**Q1**：为什么有环不一定死锁？  
**A1**：多实例资源的情况下，有环只说明存在循环等待的可能性，但可能有足够的资源实例来打破等待。只有单实例资源有环才一定死锁。

**Q2**：银行家算法的时间复杂度是多少？  
**A2**：O(m × n²)，其中 m 是资源类型数，n 是进程数。每次尝试找一个可执行进程需要 O(m×n)，最多找 n 次。

## 7. 总结复盘

- 死锁的四个条件缺一不可
- 银行家算法是死锁避免的经典方法，但需要预先知道最大需求
- 实际系统更多使用检测+恢复或鸵鸟策略（忽略死锁，让用户重启）
- **易错点**：银行家算法中 Need = Max - Allocation 而非 Max；忘记检查 Finish[i] 是否为 false

---

# 第九章 物理内存管理

## 1. 概念引入

**地址绑定（Address Binding）**：程序中的地址在不同阶段有不同的形式：
- **编译时绑定**：生成绝对代码，地址固定（如嵌入式系统）
- **加载时绑定**：加载到内存时确定地址，之后不可变
- **执行时绑定**：运行时通过硬件（MMU）动态转换地址（最灵活）

**逻辑地址（Logical/虚拟地址）vs 物理地址（Physical Address）**：
- 逻辑地址：CPU 生成的地址
- 物理地址：内存硬件看到的地址
- 执行时绑定的系统中，逻辑地址 ≠ 物理地址，需要 **MMU** 转换

## 2. 核心原理

**内存管理单元（MMU）**：
- CPU 和内存之间的硬件设备
- 将逻辑地址转换为物理地址
- 最简单方案：基址寄存器 + 限长寄存器
  - 物理地址 = 基址 + 逻辑地址
  - 检查：逻辑地址 < 限长，否则产生越界中断

**连续内存分配**：
- **固定分区**：内存分为固定大小的分区，每个分区放一个进程
  - 问题：内部碎片（分区内未使用空间）、限制进程数
- **可变分区**：按进程大小动态分配
  - 分配算法：
    - **首次适应（First Fit）**：找到第一个够大的空闲区
    - **最佳适应（Best Fit）**：找最小的够大空闲区
    - **最差适应（Worst Fit）**：找最大的空闲区
  - 问题：外部碎片（空闲区分散，总量够但不连续）

**碎片问题**：
- **内部碎片**：分配的块大于实际需求，多余空间浪费
- **外部碎片**：总空闲空间够但不连续，无法满足大块分配
- 解决外部碎片：**紧缩（Compaction）**——移动进程使空闲区合并（开销大）

## 3. 方法速通

**分页（Paging）**——非连续内存分配的核心技术：
- 将物理内存分为固定大小的 **帧（Frame）**，逻辑地址空间分为同样大小的 **页（Page）**
- 页和帧大小相同（通常 4KB）
- 进程的页可以分散在内存的不同帧中
- **优点**：无外部碎片，只有少量内部碎片（最后一页）

**地址转换**（分页）：
- 逻辑地址 = (页号, 页内偏移)
- 页号 = 逻辑地址 / 页大小（整除）
- 页内偏移 = 逻辑地址 % 页大小（取余）
- 查页表：页号 → 帧号
- 物理地址 = 帧号 × 页大小 + 页内偏移

**页表（Page Table）**：
- 每个进程一个页表
- 页表寄存器（PTBR）指向页表在内存中的起始地址
- 每次内存访问需要两次访存：一次查页表，一次访问实际数据
- 优化：**TLB（Translation Lookaside Buffer）**——高速缓存页表项

**TLB 命中率**：
- 设 TLB 命中率 p，TLB 查找时间 ε，内存访问时间 m
- 有效访问时间 = p × (ε + m) + (1-p) × (ε + 2m)

## 4. 典型示例

**分页地址转换**：
- 页大小 4KB (2^12)，逻辑地址 8196 = 0x2004
- 页号 = 8196 / 4096 = 2，偏移 = 8196 % 4096 = 4
- 查页表：页 2 → 帧 6
- 物理地址 = 6 × 4096 + 4 = 24580 = 0x6004

**页表大小计算**：
- 32 位地址空间，4KB 页大小
- 页数 = 2^32 / 2^12 = 2^20 页
- 每个页表项 4 字节 → 页表大小 = 2^20 × 4B = 4MB
- 每个进程需要 4MB 的页表！

## 5. 真题/实战

- x86-64 使用 4 级页表（PML4 → PDP → PD → PT → 页内偏移）
- Linux 中 `getconf PAGE_SIZE` 查看页大小
- 页表管理是 OS 性能关键，TLB 命中率直接影响系统性能

## 6. 巩固练习

**Q1**：为什么分页不会产生外部碎片？  
**A1**：因为物理内存被分为固定大小的帧，任何空闲帧都可以分配给任何页，不需要连续。只要总空闲帧数足够就能分配。

**Q2**：32 位系统、4KB 页、4 字节页表项，为什么需要多级页表？  
**A2**：单级页表需要 4MB（2^20 × 4B），太大且连续。多级页表通过"用到才分配"减少内存占用。

## 7. 总结复盘

- 连续分配简单但碎片问题严重
- 分页消除了外部碎片，是现代 OS 的标准方案
- TLB 是分页性能的关键
- **易错点**：地址转换时页号和偏移的计算（用除法和取余，不是位移）；混淆帧和页的概念（页是逻辑的，帧是物理的）

---

# 第十章 虚拟内存

## 1. 概念引入

**虚拟内存（Virtual Memory）**：让程序认为自己拥有连续的、足够大的内存空间，实际上只有部分页面在物理内存中，其余在磁盘上。当需要不在内存中的页面时，触发缺页异常，操作系统从磁盘调入。

**请求调页（Demand Paging）**：页面在首次被访问时才调入内存（而不是在程序启动时全部调入）。需要硬件支持：页表项中的有效/无效位（valid/invalid bit）。

## 2. 核心原理

**缺页异常（Page Fault）处理流程**：
1. CPU 访问逻辑地址，查页表发现该页不在内存（有效位 = invalid）
2. 触发缺页异常，陷入内核
3. OS 检查访问是否合法（检查进程的页表/内存映射）
4. 如果合法：在物理内存中找一个空闲帧
5. 如果无空闲帧：执行页面置换算法，选择一个"牺牲"帧
6. 如果牺牲页被修改过（脏页），写回磁盘
7. 将所需页面从磁盘读入内存
8. 更新页表，设置有效位
9. 重新执行引发缺页的指令

**页面置换算法**：

| 算法 | 描述 | 特点 |
|------|------|------|
| **FIFO** | 替换最先进入内存的页 | 简单，Belady 异常 |
| **OPT（最优）** | 替换将来最长时间不会被使用的页 | 理论最优，无法实现 |
| **LRU** | 替换最近最久未使用的页 | 接近 OPT，需硬件支持 |
| **时钟算法（Clock）** | LRU 的近似，使用引用位 | 实际系统最常用 |
| **LFU** | 替换使用频率最低的页 | 可能不适合突发访问 |

**Belady 异常**：FIFO 算法中，增加物理帧数反而可能增加缺页次数。LRU 和 OPT 不会有此异常。

## 3. 方法速通

**LRU 实现**：
- **计数器法**：每个页表项记录最后访问时间，替换时间最小的页
- **栈法**：维护一个页面号栈，访问某页时移到栈顶，替换栈底的页
- 实现开销大，实际系统多用近似算法

**时钟算法（Clock / Second Chance）**：
1. 所有帧组织成循环链表，有一个指针
2. 访问页面时设置引用位 = 1
3. 需要置换时，指针扫描：引用位=1 则清零并跳过，引用位=0 则替换该页
4. 改进：增加修改位，优先替换未修改的页（减少 I/O）

**帧分配策略**：
- **平均分配**：每个进程分相同数量的帧
- **比例分配**：按进程大小按比例分配
- **全局置换 vs 局部置换**：全局可在所有帧中选择牺牲页，局部只能在自己的帧中选择

## 4. 典型示例

**页面置换模拟**（页面引用串：7,0,1,2,0,3,0,4,2,3,0,3,2,1,2,0,1,7,0,1，3 个帧）：

| 步骤 | 访问页 | 帧状态 | 缺页？ |
|------|--------|--------|--------|
| 1 | 7 | [7,_,_] | 是 |
| 2 | 0 | [7,0,_] | 是 |
| 3 | 1 | [7,0,1] | 是 |
| 4 | 2 | [2,0,1] | 是(替换7) |
| 5 | 0 | [2,0,1] | 否 |
| ... | ... | ... | ... |

**抖动（Thrashing）**：
- 当进程的工作集（频繁使用的页面集合）大于分配给它的帧数时
- 进程不断缺页，CPU 大量时间花在页面置换上
- CPU 利用率反而下降（"颠簸"）

## 5. 真题/实战

- Linux 使用改进的时钟算法（双链表：active list + inactive list）
- `vmstat` 命令查看系统缺页率（si/so 列）
- 工作集模型：`working_set(t, Δ)` = 在时间 t 前 Δ 时间单位内访问过的页面集合

## 6. 巩固练习

**Q1**：为什么 OPT 算法无法实现？  
**A1**：OPT 需要预知未来页面的访问序列，这在实际系统中不可能。它只作为理论基准用于评估其他算法。

**Q2**：什么是工作集模型？如何用它解决抖动？  
**A2**：工作集是进程在最近 Δ 时间内使用的页面集合。如果进程的工作集大于其分配的帧数，就会抖动。解决方法：为每个进程分配至少等于其工作集大小的帧数，或减少并发进程数。

## 7. 总结复盘

- 虚拟内存让程序看到的地址空间远大于物理内存
- 请求调页 + 页面置换是虚拟内存的核心机制
- LRU 是实际效果最好的算法，时钟算法是其常用近似
- **易错点**：Belady 异常只出现在 FIFO 中；混淆缺页和 Cache miss；忘记脏页需要写回磁盘

---

# 第11章 大容量存储（Mass Storage）

## 1. 概念引入

操作系统需要管理大容量外存设备（如机械硬盘、固态硬盘），核心目标是**高效、可靠**地存储和访问数据。本章围绕三个核心问题展开：

- 磁盘的物理结构如何影响访问性能？
- 如何通过调度算法优化磁盘访问？
- 如何通过冗余技术（RAID）提升可靠性和吞吐量？

## 2. 核心公式/原理

### 2.1 磁盘结构基础

- **磁道（Track）**：盘片上的同心圆
- **扇区（Sector）**：磁道上的弧段，是最小传输单元（通常512字节）
- **柱面（Cylinder）**：所有盘片上同一半径的磁道集合
- **磁头（Head）**：每个盘面一个，负责读写
- 转速：60~250 RPS（即3600~15000 RPM）

### 2.2 磁盘性能公式

**平均I/O时间**：

```
平均I/O时间 = 平均访问时间 + 数据传输时间 + 控制器开销
平均访问时间 = 平均寻道时间 + 平均旋转延迟
平均旋转延迟 = 60 / (2 * RPM)   （磁盘旋转半周的时间）
```

**磁盘I/O传输时间**：

```
Ta = T_s + (1/(2r)) + (b/(rN))

Ta: 访问时间
T_s: 寻道时间
r: 旋转速度（转/秒）
b: 传输字节数
rN: 传输速率（N为每磁道字节数）
```

### 2.3 计算示例

在7200 RPM磁盘上传输4KB块，平均寻道时间5ms，传输速率1Gb/s，控制器开销0.1ms：

- 平均旋转延迟 = 60 / (2 x 7200) x 1000 = 4.17ms
- 传输时间 = 4KB / (1Gb/s) = 0.031ms
- 平均I/O时间 = 5 + 4.17 + 0.1 + 0.031 = 9.301ms

**关键结论**：寻道时间和旋转延迟占绝对主导，传输时间几乎可忽略。

## 3. 方法速通

### 3.1 磁盘调度算法

当多个I/O请求排队时，调度算法决定服务顺序，目标是**最小化寻道时间**、**最大化磁盘带宽**。

#### 各算法速记

| 算法 | 英文全称 | 核心思想 | 优点 | 缺点 |
|------|----------|----------|------|------|
| **FCFS** | First Come First Served | 按请求到达顺序服务 | 公平，实现简单 | 性能差，接近随机调度 |
| **SSTF** | Shortest Seek Time First | 选择离当前磁头最近的请求 | 平均寻道时间短 | 可能导致饥饿 |
| **SCAN** | SCAN (电梯算法) | 磁头单向移动，到端点后反向 | 服务分布较好，减少饥饿 | 对两端磁道请求有利，中间请求不利 |
| **C-SCAN** | Circular SCAN | 单向扫描，到端点后快速返回起点重新开始 | 服务更均匀，等待时间方差小 | 返回过程不处理请求 |
| **LOOK** | LOOK | 类似SCAN，但不必到端点，无请求时即可反向 | 比SCAN更高效 | 仍有方向偏好 |
| **C-LOOK** | Circular LOOK | 类似C-SCAN，但不必到端点 | 比C-SCAN更高效 | 返回过程不处理请求 |
| **N-step-SCAN** | N-step SCAN | 将请求队列分成长度为N的子队列，依次SCAN | 避免长队列导致的延迟不均 | 参数N需选择 |
| **FSCAN** | FSCAN | 使用两个子队列，扫描期间新请求放入另一队列 | 防止磁头粘滞 | 实现稍复杂 |

#### 示例对比

请求序列（初始磁头在100号磁道）：55, 58, 39, 18, 90, 160, 150, 38, 184

| 算法 | 平均寻道长度 |
|------|-------------|
| FCFS | 55.3 |
| SSTF | 27.5 |
| SCAN | 27.8 |
| C-SCAN | 35.8 |

**选型建议**：
- SSTF最常用，性能好
- SCAN/C-SCAN适合磁盘负载较大的系统，减少饥饿
- Linux使用**截止时间调度器**确保每个请求在一定时间内被服务

### 3.2 SSD调度

- SSD无磁头、无旋转延迟，传统磁盘调度算法不适用
- 使用NOOP（无操作调度），仅合并相邻LBA请求
- SSD随机IOPS远高于HDD（几十万 vs 几百），但存在**写放大**问题

## 4. 典型示例

### 4.1 磁盘格式化三步骤

1. **低级格式化（物理格式化）**：将磁盘划分为扇区，每个扇区包含头信息、数据和ECC
2. **分区**：将磁盘划分为柱面集合，每个集合视为独立逻辑磁盘
3. **逻辑格式化（创建文件系统）**：操作系统将文件系统数据结构存储到磁盘（空闲/已分配空间、空目录）

### 4.2 坏块处理

- **简单磁盘**：格式化时扫描标记坏块，运行时用`badblocks`命令锁定
- **复杂磁盘（扇区备用）**：控制器维护坏块列表，用备用块逻辑替换坏块
- **扇区滑动**：扇区备用的替代方案

## 5. 真题/实战

### 5.1 RAID 级别对比（重点）

| 级别 | 名称 | 所需磁盘 | 冗余方式 | 读性能 | 写性能 | 可靠性 | 特点 |
|------|------|----------|----------|--------|--------|--------|------|
| **RAID 0** | 条带化（无冗余） | N | 无 | 极高 | 极高 | 低于单盘 | 不是真正的RAID，性能最优但无容错 |
| **RAID 1** | 镜像 | 2N | 数据镜像 | 高于单盘 | 约等于单盘 | 高 | 成本最高，重建最简单 |
| **RAID 2** | 海明码 | N+m | 海明码纠错 | 最高 | 最高 | 高 | 实际中不使用 |
| **RAID 3** | 位交错奇偶校验 | N+1 | 单盘奇偶校验 | 最高 | 最高 | 高 | 并行访问，适合大数据传输 |
| **RAID 4** | 块交错奇偶校验 | N+1 | 单盘奇偶校验 | 读类似RAID 0 | 写有惩罚（小写） | 高 | 独立访问，奇偶校验盘成为瓶颈 |
| **RAID 5** | 块交错分布奇偶校验 | N+1 | 分布式奇偶校验 | 读类似RAID 0 | 写低于单盘 | 高 | 最常用，消除RAID 4的写瓶颈 |
| **RAID 6** | 双重分布式奇偶校验 | N+2 | 双重冗余 | 读类似RAID 0 | 写低于RAID 5 | 所有级别中最高 | 可容忍两块磁盘同时故障 |

**RAID 0+1 vs RAID 1+0**：
- RAID 0+1：先分条再镜像。一条中一块盘故障，整条不可用
- RAID 1+0：先镜像再分条。单盘故障，其镜像仍可用，容错更优

**RAID实现层次**：
- 软件RAID（卷管理器，性能较低）
- 主机总线适配器（HBA）硬件
- 存储阵列硬件
- SAN虚拟化设备

### 5.2 存储连接方式

| 方式 | 说明 |
|------|------|
| **主机连接存储（DAS）** | 通过本地I/O端口访问（IDE、ATA、SATA等） |
| **网络连接存储（NAS）** | 通过网络远程访问，使用RPC（NFS、CIFS），基于TCP/IP |
| **存储区域网络（SAN）** | 专用存储网络，使用存储协议（非网络协议），常见FC和iSCSI |

### 5.3 磁盘管理要点

- **引导块（Boot Block）**：存储引导加载程序（MBR），ROM中的自举程序负责初始化系统
- **交换空间管理**：用于虚拟内存的换入换出，可在文件系统文件或专用原始分区中实现；高估比低估安全
- **稳定存储**：每个逻辑块维护两个物理副本，确保写入过程中故障不会损坏所有副本

## 6. 巩固练习

**题目1**：某磁盘转速为10000 RPM，平均寻道时间6ms，每磁道500个扇区，每扇区512字节。求读取一个扇区的平均I/O时间。

**解答**：
- 平均旋转延迟 = 60 / (2 x 10000) x 1000 = 3ms
- 每转时间 = 60/10000 = 6ms，每扇区传输时间 = 6/500 = 0.012ms
- 平均I/O时间 = 6 + 3 + 0.012 = 9.012ms

**题目2**：给定请求序列（磁头初始在53）：98, 183, 37, 122, 14, 124, 65, 67，分别用FCFS、SSTF、SCAN计算总寻道距离。

**解答**：
- FCFS: |53-98|+|98-183|+|183-37|+|37-122|+|122-14|+|14-124|+|124-65|+|65-67| = 45+85+146+85+108+110+59+2 = 640
- SSTF: 53->65->67->37->14->98->122->124->183 = 12+2+30+23+84+24+2+59 = 236
- SCAN(向磁道号增大方向): 53->65->67->98->122->124->183->37->14 = 12+2+31+24+2+59+146+23 = 299

**题目3**：4块盘组成的RAID 5，每块盘8个块。P(4-7)在哪个盘上？

**解答**：RAID 5使用轮转分布奇偶校验。假设P(0-3)在盘3、P(4-7)在盘2、P(8-11)在盘1、P(12-15)在盘0（具体轮转顺序取决于实现，但核心原则是奇偶校验块分散在所有盘上）。

## 7. 总结复盘

### 易错点

- 旋转延迟是**旋转半周**的时间，不是一周
- 寻道时间远大于传输时间，优化重点在减少寻道
- RAID 0没有冗余，严格来说不算RAID
- SSD不需要磁盘调度算法，但有写放大问题

### 关键对比

| 对比项 | HDD | SSD |
|--------|-----|-----|
| 寻道延迟 | 有（3~12ms） | 无 |
| 旋转延迟 | 有 | 无 |
| 可靠性 | 有机械部件，较低 | 无移动部件，较高 |
| 功耗 | 较高 | 较低 |
| 每MB成本 | 较低 | 较高 |
| 寿命 | 较长 | 写入次数有限 |
| 调度优化 | 需要 | 不需要（仅合并请求） |

---

# 第12章 I/O系统

## 1. 概念引入

I/O系统是操作系统的核心子系统之一，负责协调种类繁多、速度各异的外部设备与CPU/内存之间的数据传输。核心问题：

- 如何抽象和统一不同设备的访问方式？
- 如何高效传输数据（减少CPU负担）？
- 内核I/O子系统提供哪些服务？

## 2. 核心公式/原理

### 2.1 I/O硬件基础

**I/O端口四类寄存器**：
- **数据输入寄存器**：主机读取数据
- **数据输出寄存器**：主机写入数据
- **状态寄存器**：反映设备状态（完成、数据就绪、故障等）
- **控制寄存器**：主机写入以启动命令或更改模式

**CPU与设备通信方式**：
- **I/O指令**：专用指令访问I/O端口地址（如`out 0x21, AL`）
- **内存映射I/O（Memory-mapped I/O）**：设备寄存器映射到物理内存地址空间，用标准load/store指令访问

### 2.2 三种数据传输技术

| 技术 | 方式 | CPU参与 | 适用场景 | 效率 |
|------|------|---------|----------|------|
| **轮询（Polling/PIO）** | CPU循环检查状态寄存器 | 全程参与 | 低速设备、设备速度匹配时 | 低速设备合理，高速设备浪费 |
| **中断驱动I/O** | 设备就绪后发中断通知CPU | 数据传输时参与 | 通用场景 | 中等，减少等待 |
| **DMA** | DMA控制器直接在设备与内存间传输 | 仅启动和结束时参与 | 大容量传输（磁盘） | 高效，但"窃取"CPU总线周期 |

### 2.3 DMA工作原理

1. CPU将DMA命令块写入内存（含源地址、目标地址、传输字节数）
2. CPU将命令块地址写入DMA控制器
3. DMA控制器直接操作内存总线，逐块传输数据（窃取CPU周期）
4. 传输完成后，DMA控制器向CPU发送中断

## 3. 方法速通

### 3.1 I/O设备分类

按数据传输特性分为：

| 类型 | 接口 | 典型设备 | 系统调用 |
|------|------|----------|----------|
| **块设备** | 以块为单位传输 | 磁盘、光盘 | read(), write(), seek() |
| **字符设备** | 以字符流传输 | 键盘、鼠标、串口 | get(), put() |
| **网络设备** | 套接字接口 | 网卡 | socket(), select() |
| **时钟/定时器** | 特殊接口 | 硬件时钟 | 获取时间、设置定时器 |

### 3.2 I/O操作模式

| 模式 | 特点 | 返回时机 |
|------|------|----------|
| **阻塞I/O** | 进程挂起直到I/O完成 | I/O完成后返回 |
| **非阻塞I/O** | 尽可能多地返回已就绪数据 | 立即返回部分数据 |
| **异步I/O** | 进程继续运行，I/O完成时通知 | 立即返回，后续信号通知 |

**区别**：非阻塞read()立即返回可用数据（可能少于请求量）；异步read()要求完整执行，但完成通知在将来某时。

### 3.3 向量I/O（Vectored I/O / Scatter-Gather）

- 一次系统调用执行涉及多个缓冲区位置的多个I/O操作
- 减少上下文切换和系统调用开销
- 可提供原子性，避免多线程数据损坏

## 4. 典型示例

### 4.1 I/O请求生存周期

```
用户进程 -> 系统调用 -> 内核I/O子系统（判断缓存命中）
  |-> 缓存命中 -> 直接返回结果
  |-> 未命中 -> 设备驱动 -> 发送控制命令到设备控制器
     -> 设备完成 -> 产生中断 -> 中断处理例程
     -> 通知设备驱动 -> 通知I/O子系统 -> 返回用户进程
```

### 4.2 轮询握手过程（输出）

1. 主机反复读取状态寄存器的忙位，直到清零（忙等待）
2. 主机设置命令寄存器的写位，写数据到数据输出寄存器
3. 主机设置命令就绪位
4. 控制器注意到就绪位，设置忙位
5. 控制器执行I/O操作
6. 控制器清除就绪位、故障位、忙位，表示完成

### 4.3 中断处理

- **中断向量**：包含中断处理程序地址的表（Intel: 0-31非屏蔽中断，32-255可屏蔽中断）
- **非屏蔽中断（NMI）**：不可恢复的内存错误等严重事件
- **可屏蔽中断**：设备请求服务，CPU可关闭
- **中断链**：多个设备共享同一中断号时的处理方式

## 5. 真题/实战

### 5.1 内核I/O子系统服务

| 服务 | 说明 |
|------|------|
| **I/O调度** | 确定I/O请求执行顺序，通过设备队列管理 |
| **缓冲（Buffering）** | 在设备间传输时临时存储数据于内存中；解决设备速度不匹配、传输大小不匹配、维护拷贝语义 |
| **缓存（Caching）** | 在更快设备中保存数据副本；是性能的关键；可与缓冲结合 |
| **假脱机（Spooling）** | 保存设备输出的缓冲区，用于一次只能服务一个请求的设备（如打印机） |
| **设备预留** | 提供设备的独占访问，需注意死锁 |
| **错误处理** | 重试读写、记录错误日志、频率追踪 |

### 5.2 I/O保护

- 所有I/O指令定义为**特权指令**
- I/O必须通过系统调用执行
- 内存映射和I/O端口地址空间必须受保护

### 5.3 I/O性能优化

- 减少上下文切换
- 减少数据复制
- 使用大型传输、智能控制器和轮询减少中断
- 使用DMA
- 平衡CPU、内存、总线和I/O性能
- 将用户态守护进程移至内核线程

### 5.4 设备命名

- **MS-DOS**：文件名中用冒号标识设备（如`C:`）
- **Unix/Linux**：通过安装表将路径前缀与设备关联；设备有主/次设备号
- **Unix ioctl()**：向设备控制寄存器发送任意命令的"后门"机制

## 6. 巩固练习

**题目1**：比较轮询、中断和DMA三种I/O方式的CPU占用率。

**解答**：
- 轮询：CPU全程参与，占用率最高
- 中断：CPU在等待期间可执行其他任务，但每次数据传输都需要CPU参与，占用率中等
- DMA：CPU仅在传输启动和完成中断时参与，大块传输时CPU占用率最低

**题目2**：为什么说缓冲区维护了"拷贝语义（copy semantics）"？

**解答**：应用程序调用write()时，系统将数据复制到内核缓冲区。此后即使应用程序修改了用户空间的缓冲区，写到磁盘的仍是系统调用时刻的那份数据副本。这保证了写入数据的正确性和一致性。

**题目3**：非阻塞I/O与异步I/O的核心区别是什么？

**解答**：非阻塞I/O调用立即返回当前可用的数据（读取量可能小于请求量）；异步I/O调用也会立即返回，但请求的数据会被完整地在后台执行，系统在将来某个时间通知进程I/O已完成。

## 7. 总结复盘

### 易错点

- 轮询在设备速度快且持续就绪时效率高，不是在所有情况下都低效
- 缓冲（Buffering）和缓存（Caching）概念不同：缓冲是临时存储正在传输的数据，缓存是保存已使用数据的副本
- DMA虽然高效，但在占用内存总线时会"窃取"CPU周期，减慢CPU计算
- 异步I/O比非阻塞I/O更难编程，但数据传输更完整

### 关键对比

| 对比项 | 缓冲（Buffering） | 缓存（Caching） |
|--------|-------------------|-----------------|
| 目的 | 解决速度/大小不匹配 | 提高访问速度 |
| 数据 | 正在传输的数据 | 已使用数据的副本 |
| 副本 | 唯一副本（可能） | 总是副本，原始可丢弃 |

| 对比项 | 阻塞I/O | 非阻塞I/O | 异步I/O |
|--------|---------|-----------|---------|
| 进程状态 | 挂起等待 | 不挂起 | 不挂起 |
| 返回数据 | 完整数据 | 部分/可用数据 | 后续通知完成 |
| 编程难度 | 低 | 中 | 高 |

---

# 第13章 文件系统（File System Interface）

## 1. 概念引入

文件（File）是操作系统对存储设备物理属性的抽象，为用户提供信息存储的统一逻辑视图。文件系统的核心职责：

- 定义文件的逻辑结构和操作
- 组织目录结构以管理大量文件
- 提供文件共享和保护机制

## 2. 核心公式/原理

### 2.1 文件属性

| 属性 | 说明 |
|------|------|
| 名称 | 人类可读的文件标识 |
| 标识符 | 文件系统内的唯一标签（不可见） |
| 类型 | 文件类型信息 |
| 位置 | 设备上的文件位置指针 |
| 尺寸 | 文件大小 |
| 保护 | 访问控制信息 |
| 时间/日期/用户 | 创建、修改、访问信息 |

### 2.2 文件操作

最小文件操作集合：**创建、删除、读、写、重新定位（搜索）、截断**

每个打开文件关联以下信息（**打开文件表**）：
- **文件指针**：跟踪读写位置，每进程唯一
- **文件打开计数**：跟踪打开/关闭次数
- **文件的磁盘位置**
- **访问权限**

### 2.3 文件锁

| 锁类型 | 类比 | 说明 |
|--------|------|------|
| **共享锁** | 读者锁 | 多个进程可并发获取 |
| **独占锁** | 写者锁 | 一次仅一个进程可获取 |

- **强制锁**：操作系统强制独占访问
- **建议锁**：非独占，进程可选择是否遵守

## 3. 方法速通

### 3.1 文件类型识别

| 系统 | 识别方式 |
|------|----------|
| Windows | 文件扩展名（如`.exe`、`.txt`） |
| Unix/Linux | 文件头部的magic数字 |

**Unix文件观**：所有文件视为8位字节序列（字节流），不解释位的含义，提供最大灵活性。

### 3.2 内部文件结构

- 逻辑记录打包到物理块中
- 文件可看作块的序列，所有基本I/O以块为单位
- 最后一块可能产生**内部碎片**（如512字节块存储1949字节文件需4块，浪费99字节）

### 3.3 文件访问方法

| 访问方式 | 说明 | 操作 | 适用场景 |
|----------|------|------|----------|
| **顺序访问** | 按记录顺序处理 | read_next(), write_next() | 编辑器、编译器 |
| **直接访问（相对访问）** | 按任意顺序读写固定长度记录 | read(n), write(n) | 数据库 |
| **索引访问** | 建立索引加速查找 | 索引 + 二分搜索 | 大型数据库 |

**关键**：支持直接访问的系统可以轻松模拟顺序访问（维护当前位置变量cp），反之则低效。

## 4. 典型示例

### 4.1 目录结构对比

| 结构 | 说明 | 优点 | 缺点 |
|------|------|------|------|
| **单级目录** | 所有文件在一个目录 | 简单 | 文件命名冲突，难以管理 |
| **两级目录** | 每用户一个UFD，系统有MFD | 解决命名冲突 | 用户间协作困难 |
| **树形目录** | 任意高度的树 | 层次清晰，支持绝对/相对路径 | 不支持文件共享 |
| **无环图目录** | 允许共享子目录和文件 | 支持共享 | 需处理悬挂指针、环检测 |
| **通用图目录** | 允许有环图 | 最灵活 | 必须防止环，实现复杂 |

### 4.2 无环图的共享问题

**软链接 vs 硬链接**：
- **硬链接**：直接指向同一文件的inode，删除原文件不影响链接
- **软链接（符号链接）**：指向文件路径名的间接指针，原文件删除后链接失效

**防环策略**：
- 只允许链接到文件而非子目录
- 使用垃圾收集
- 每次添加链接时进行循环检测

### 4.3 文件系统安装（Mount）

安装过程：
1. 操作系统知道设备名和**安装点（mount point）**（通常是空目录）
2. 验证设备包含有效文件系统
3. 在目录结构中记录安装信息

### 4.4 一致性语义

| 语义 | 说明 |
|------|------|
| **Unix语义** | 一个用户的写入对其他打开同一文件的用户**立即可见** |
| **Andrew（AFS）语义** | 写入在文件**关闭后**才对新会话可见 |

## 5. 真题/实战

### 5.1 文件保护

- **访问类型**：读、写、执行、附加、删除、列表
- **访问控制**：每文件/目录关联访问控制列表（ACL），区分三种用户类型：
  - **所有者（Owner）**
  - **组（Group）**
  - **其他用户（Others）**
- Unix使用9位保护模式（rwxrwxrwx）

### 5.2 文件共享

- **多用户系统**：需要文件所有者和组的概念
- **远程文件系统**：FTP、分布式文件系统（DFS）、万维网
- **NFS**：基于客户机-服务器模型的网络文件共享

## 6. 巩固练习

**题目1**：为什么Unix将所有文件视为字节流？这种设计有什么优缺点？

**解答**：
- 优点：最大灵活性，应用程序可自由解释文件内容，无需操作系统支持多种文件结构
- 缺点：每个应用程序必须包含自己的文件结构解释代码，缺乏统一支持
- 例如加密文件既非文本也非可执行，字节流设计避免了类型限制问题

**题目2**：树形目录和无环图目录的核心区别是什么？

**解答**：树形目录中每个文件/目录只能有一个父目录，不允许共享；无环图目录允许同一文件出现在两个不同目录中（一个用户修改立即对其他用户可见），但需要处理悬挂指针等问题。

**题目3**：什么是安装点？在Unix和Windows中安装有什么区别？

**解答**：安装点是将文件系统接入目录树的入口目录。Unix允许将文件系统安装在任意目录上（如`/home`），Windows为每个卷分配独立的名称空间（如`C:`、`F:`）。

## 7. 总结复盘

### 易错点

- 文件标识符（ID）和文件名不同：标识符是系统内部唯一标签，名称是人类可读的
- 直接访问可以模拟顺序访问（维护指针），但顺序访问模拟直接访问非常低效
- 共享锁类似读者锁（多个进程可同时获取），独占锁类似写者锁（互斥）
- Andrew语义中写入对新会话可见（非实时），Unix语义中写入立即可见

### 关键对比

| 对比项 | 顺序访问 | 直接访问 |
|--------|----------|----------|
| 访问模式 | 按序逐个 | 任意顺序 |
| 操作 | read_next() | read(n) |
| 适用场景 | 文本编辑、编译 | 数据库查询 |
| 模拟顺序 | N/A | 容易（维护指针cp） |
| 模拟直接 | 低效且笨拙 | N/A |

---

# 第14章 文件系统实现（File System Implementation）

## 1. 概念引入

文件系统实现涉及如何在磁盘和内存中组织数据结构以支持文件操作。核心问题：

- 磁盘上的文件系统结构如何布局？
- 如何高效分配和管理磁盘空间？
- 如何在系统崩溃后恢复一致性？

## 2. 核心公式/原理

### 2.1 文件系统层次结构

```
应用程序
  |
逻辑文件系统（管理元数据：FCB、目录结构）
  |
文件组织模块（逻辑块地址 -> 物理块地址）
  |
基本文件系统（向设备驱动发送通用命令）
  |
I/O控制层（设备驱动程序和中断处理程序）
```

### 2.2 磁盘上的文件系统结构

每个卷/分区包含：

| 结构 | 说明 | UFS名称 | NTFS名称 |
|------|------|---------|----------|
| **引导控制块** | 从该卷引导OS的信息 | 引导块（Boot Block） | 分区引导扇区 |
| **卷控制块** | 块数、块大小、空闲块等 | 超级块（Superblock） | 主控文件表（MFT） |
| **目录结构** | 组织文件 | 文件名 + inode号 | MFT中的条目 |
| **文件控制块（FCB）** | 每文件的详细信息 | inode | MFT条目 |

### 2.3 内存中的文件系统结构

| 结构 | 说明 |
|------|------|
| **安装表（Mount Table）** | 每个已安装卷的信息 |
| **目录结构缓存** | 最近访问的目录信息 |
| **系统级打开文件表** | 每个打开文件的FCB副本 |
| **进程级打开文件表** | 指向系统级表的指针（Unix称为文件描述符，Windows称为文件句柄） |
| **缓冲区缓存** | 文件系统块的缓存 |

### 2.4 虚拟文件系统（VFS）

VFS提供两个关键功能：

1. **统一接口**：将通用操作与具体实现分离，允许多种文件系统共存
2. **网络文件标识**：基于vnode（虚拟节点）唯一标识网络上的文件

**Linux VFS四种对象**：
- **索引节点对象（inode object）**：表示一个单独文件
- **文件对象（file object）**：表示一个已打开文件
- **超级块对象（superblock object）**：表示整个文件系统
- **目录条目对象（dentry object）**：表示单个目录条目

## 3. 方法速通

### 3.1 目录实现

| 方法 | 说明 | 优点 | 缺点 |
|------|------|------|------|
| **线性列表** | 文件名 + 指针的线性表 | 实现简单 | 查找慢（线性搜索） |
| **哈希表** | 线性列表 + 哈希结构 | 查找快 | 有冲突问题，需要固定大小条目或链式溢出 |

改进：有序链表、B+树

### 3.2 文件分配方法（重点）

#### 连续分配（Contiguous Allocation）

- 文件占据一组**连续**的磁盘块
- 目录记录起始块号和长度
- **优点**：性能最优（寻道最少），支持顺序和直接访问，实现简单
- **缺点**：外部碎片，需要预知文件大小或动态扩展困难，需要碎片整理（compaction）
- **改进**：**基于扩展的系统（Extent-based）**：初始分配连续空间，不够时添加新的连续扩展

#### 链接分配（Linked Allocation）

- 每个文件是磁盘块的**链表**，块可散布在磁盘任何位置
- 目录记录起始块号，每块末尾存指向下一块的指针
- **优点**：无外部碎片，无需预知文件大小
- **缺点**：仅适合顺序访问（不支持直接访问），指针占用空间，可靠性问题（指针损坏则文件丢失）
- **变种**：**文件分配表（FAT）**：将所有块指针集中存放在磁盘开头的表中，目录仅记录首块号

#### 索引分配（Indexed Allocation）

- 将所有块指针集中到**索引块**
- 每个文件有自己的索引块
- **优点**：支持直接访问，无外部碎片
- **缺点**：索引块本身占用空间，小文件浪费索引块

**索引块扩展方案**：

| 方案 | 说明 |
|------|------|
| **链接方案** | 多个索引块链接成链 |
| **多级索引** | 一级索引指向二级索引，以此类推（可多级） |
| **组合方案（Unix inode）** | inode中直接地址（前12个指针指向直接块）+ 一级间接 + 二级间接 + 三级间接 |

#### 组合方案计算示例

inode有12个地址项（8直接 + 2一级间接 + 1二级间接 + 1三级间接），地址项4字节，块大小1KB：

```
每块可存指针数 = 1024 / 4 = 256

直接地址容量: 8 x 1KB = 8KB
一级间接容量: 2 x 256 x 1KB = 512KB
二级间接容量: 256 x 256 x 1KB = 65536KB (64MB)
三级间接容量: 256 x 256 x 256 x 1KB = 16777216KB (16GB)

总容量约为 16GB
```

#### 分配方法对比表

| 特性 | 连续分配 | 链接分配 | 索引分配 |
|------|----------|----------|----------|
| 访问方式 | 顺序 + 直接 | 仅顺序 | 顺序 + 直接 |
| 外部碎片 | 有 | 无 | 无 |
| 文件大小预知 | 需要 | 不需要 | 不需要 |
| 可靠性 | 高 | 低（指针损坏） | 中 |
| 实现复杂度 | 简单 | 中等 | 较复杂 |
| 适用场景 | 大文件顺序读写 | 小文件、顺序访问 | 通用（Unix/Linux/NTFS） |
| 典型系统 | 早期系统 | FAT | ext4, NTFS |

### 3.3 空闲空间管理

| 方法 | 说明 | 优点 | 缺点 |
|------|------|------|------|
| **位向量（Bit Map）** | 每块一位（0空闲/1已分配） | 易找连续空间，查找快 | 需额外空间（1TB磁盘+4KB块约需32MB） |
| **链表（Linked List）** | 空闲块用指针链接 | 无空间浪费 | 难找连续空间，需多次I/O |
| **组（Grouping）** | 每个空闲块存N个空闲块地址 | 快速找到大量空闲块 | 实现稍复杂 |
| **计数（Counting）** | 记录首个空闲块地址 + 后续连续空闲块数 | 适合连续分配，列表更短 | 每条目空间更大 |

**位向量空间计算示例**：
- 块大小4KB，磁盘1TB：n = 2^40 / 2^12 = 2^28位 = 32MB
- 若用4块簇：n减少为2^26位 = 8MB

**ZFS空间图（Space Maps）**：
- 将设备空间划分为metaslab单元
- 每个metaslab有关联的空间图，使用计数算法
- 记录到日志文件，活动时加载到平衡树结构

## 4. 典型示例

### 4.1 文件打开过程

1. 应用程序调用`open()`
2. 逻辑文件系统在系统级打开文件表中查找是否已打开
3. 若未打开：读取目录结构，找到FCB，复制到系统级打开文件表
4. 在进程级打开文件表中创建条目，指向系统级表中的FCB
5. 返回文件描述符（Unix）/文件句柄（Windows）

### 4.2 性能优化技术

| 技术 | 说明 |
|------|------|
| **缓冲区缓存（Buffer Cache）** | 主内存中存放常用磁盘块 |
| **同步写** | 无缓冲，写入必须到达磁盘后才确认 |
| **异步写** | 可缓冲，写入先到缓存再异步到磁盘（更常用、更快） |
| **随后释放（Free-behind）** | 读取后立即释放不再需要的页面 |
| **预先读取（Read-ahead）** | 预测性地读取后续块以优化顺序访问 |
| **统一缓冲区缓存** | 使用同一页面缓存缓存内存映射页面和普通文件系统I/O，避免双重缓存 |

### 4.3 恢复机制

| 机制 | 说明 |
|------|------|
| **一致性检查** | 比较目录结构与磁盘数据块，修复不一致 |
| **日志结构文件系统（LFS）** | 每个元数据更新记录为事务，写入日志；崩溃后重放未完成事务 |
| **备份与恢复** | 定期备份到其他存储设备 |

**日志文件系统工作流程**：
1. 事务写入日志（顺序写，高效）
2. 事务被标记为已提交
3. 异步将事务应用到文件系统
4. 完成后从日志中删除事务
5. 崩溃恢复时重放日志中剩余事务

## 5. 真题/实战

### 5.1 VFS的作用与实现

VFS（Virtual File System）是操作系统中实现多种文件系统共存的关键抽象层：
- 定义统一接口（open、read、write、close等）
- 使用vnode/vfs数据结构区分本地和远程文件
- 本地请求通过文件系统类型调用特定操作
- 远程请求通过NFS协议处理

### 5.2 NFS网络文件系统

- 基于客户机-服务器模型
- 每台机器可同时是客户机和服务器
- 设计目标：支持异构环境（不同机器、OS、网络架构）
- 两个协议：**安装协议**（挂载远程文件系统）和**NFS协议**（远程文件访问）
- 均基于RPC（远程过程调用）实现

### 5.3 分区与安装

| 概念 | 说明 |
|------|------|
| **原生分区** | 无文件系统 |
| **原始磁盘** | 应用自行管理数据（如数据库） |
| **根分区** | 包含OS内核，启动时安装 |
| **卷** | 可以是设备的一部分、整个设备、或RAID集 |

Unix：在目录inode的内存副本上加标志表示安装点。Windows：每个卷安装在独立名称空间（如`C:`）。

## 6. 巩固练习

**题目1**：某inode有15个地址项，其中12个直接地址、1个一级间接、1个二级间接、1个三级间接，每个地址项4字节，块大小4KB。计算最大单个文件大小。

**解答**：
- 每块指针数 = 4096 / 4 = 1024
- 直接: 12 x 4KB = 48KB
- 一级间接: 1024 x 4KB = 4MB
- 二级间接: 1024 x 1024 x 4KB = 4GB
- 三级间接: 1024 x 1024 x 1024 x 4KB = 4TB
- 总计约 4TB

**题目2**：连续分配、链接分配和索引分配分别适合什么场景？

**解答**：
- 连续分配：文件大小已知且固定，大量顺序读写（如视频文件）
- 链接分配：文件动态增长，仅需顺序访问（如日志文件），FAT系统
- 索引分配：需要直接访问的通用场景（如数据库文件），现代操作系统广泛使用

**题目3**：日志文件系统相比传统文件系统在崩溃恢复方面有什么优势？

**解答**：传统文件系统崩溃后需要遍历整个文件系统检查一致性（很慢且可能失败）。日志文件系统只需重放日志中未完成的事务，恢复速度快且消除了元数据不一致的可能性。

**题目4**：位向量管理空闲空间，磁盘8TB，块大小8KB，计算位向量大小。

**解答**：
- 总块数 = 8TB / 8KB = 2^43 / 2^13 = 2^30 块
- 位向量 = 2^30 位 = 2^27 字节 = 128MB

## 7. 总结复盘

### 易错点

- inode直接地址指针直接指向数据块，间接地址指针指向索引块（不是数据块）
- FAT是链接分配的变种，不是独立的分配方法；它将链接指针集中存放在表中，既保留了链接分配的优点，又提高了直接访问能力
- 日志文件系统不是用来替代文件系统的，而是在原有文件系统上增加崩溃恢复能力
- VFS不是具体文件系统，而是抽象层，允许多种文件系统透明共存

### 关键对比

| 对比项 | 传统FS | 日志文件系统 |
|--------|--------|-------------|
| 崩溃恢复 | 遍历整个FS检查一致性 | 重放日志事务 |
| 恢复速度 | 慢 | 快 |
| 写入开销 | 正常 | 额外日志写入（顺序，开销小） |
| 元数据一致性 | 可能不一致 | 有保障 |
| 代表 | ext2 | ext3/ext4, NTFS, ZFS |

| 对比项 | 缓冲区缓存 | 统一缓冲区缓存 |
|--------|-----------|---------------|
| 缓存对象 | 磁盘块 | 内存映射页面 + 磁盘块 |
| 双重缓存 | 可能存在 | 避免 |
| 效率 | 较低 | 较高 |
| 现代系统 | 旧系统 | 新系统（Linux 2.4+） |

### 四章知识总览

```
大容量存储（Ch11）：磁盘结构 -> 调度算法 -> RAID -> SSD
    |
I/O系统（Ch12）：硬件 -> 传输方式(轮询/中断/DMA) -> 子系统服务(缓冲/缓存/假脱机)
    |
文件系统接口（Ch13）：文件概念 -> 访问方法 -> 目录结构 -> 共享/保护
    |
文件系统实现（Ch14）：磁盘结构 -> VFS -> 分配方法 -> 空闲管理 -> 日志/恢复 -> NFS
```


---

# 附录：关键术语速查表

| 英文术语 | 中文 | 所属章节 |
|----------|------|----------|
| Process Control Block (PCB) | 进程控制块 | 4 |
| Thread Control Block (TCB) | 线程控制块 | 5 |
| System Call | 系统调用 | 2 |
| Interrupt | 中断 | 3 |
| Exception | 异常 | 3 |
| Copy-on-Write (COW) | 写时拷贝 | 4 |
| Semaphore | 信号量 | 7 |
| Monitor | 管程 | 7 |
| Deadlock | 死锁 | 8 |
| Page Table | 页表 | 9 |
| Translation Lookaside Buffer (TLB) | 转换检测缓冲 | 9 |
| Demand Paging | 请求调页 | 10 |
| Page Fault | 缺页异常 | 10 |
| Thrashing | 抖动 | 10 |
| RAID | 独立磁盘冗余阵列 | 11 |
| inode | 索引节点 | 14 |
| Journaling | 日志 | 14 |

---

> **整理完毕** — 覆盖操作系统 14 个核心章节，已去除课程安排、个人介绍、历史八卦等水分内容，保留全部技术知识点。
